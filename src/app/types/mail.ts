export interface Mail {
  name: string
  email: string
  address: string
  postalCode: string
  telephoneNumber: string
  message: string
}

export interface CreateMail {
  name: string
  email: string
  address: string
  postalCode: string
  telephoneNumber: string
  message: string
}
